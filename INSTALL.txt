==========================================================
BOW BACKEND - UPDATE: REPOSITORY PATTERN (flow 1 / design pattern)
==========================================================

APA YANG BERUBAH (lanjutan dari multi-module):
- TAMBAH 4 file baru di bow-shared:
    DataRepository.java            (interface akses data)
    SupabaseDataRepository.java    (implementasi Supabase REST)
    StorageRepository.java         (interface akses file)
    SupabaseStorageRepository.java (implementasi Supabase Storage)
- DataController & StorageController DIRAMPINGKAN:
    sekarang cuma ngurus HTTP, lalu delegasi ke repository.
- Tidak ada perubahan fungsi. Endpoint & perilaku tetap sama persis.
- Manfaat: "cara ambil data" terpisah dari "controller". Kalau suatu
  hari ganti dari Supabase REST ke JDBC, cukup ganti 1 file implementasi,
  controller tidak perlu disentuh.

CARA PASANG (lebih singkat dari kemarin, TIDAK perlu hapus src):

1. Backup dulu (zip folder bow-backend-v2) - opsional tapi disarankan.

2. Extract paket ini, lalu COPY semua isinya ke:
   C:\Users\harij\bow-backend-v2
   Saat ditanya "replace file?" -> pilih YA / Replace all.
   (File controller lama ketimpa yang baru, 4 file repository nambah.)

3. Tes build:
     cd C:\Users\harij\bow-backend-v2
     set PATH=%PATH%;C:\maven\apache-maven-3.9.16\bin
     mvn clean package -DskipTests
   Cari: BUILD SUCCESS

4. Push:
     git add .
     git commit -m "Flow 1: terapkan Repository pattern (data + storage)"
     git push

5. Tunggu Railway ACTIVE, lalu cek:
     https://bow-backend-v2-production.up.railway.app/api/hello
   Harus tetap muncul: BOW Barbershop Backend is running!
